MDP : vagrant

sudo usermod -a -G vboxsf lsouquie
sudo snap install --classic code
cd ../../media/sf_Inception/

a faire:

- Changer le nom des images docker (avec la directive Image dans le docker compose)
- ajouter un utilisateur (voir wp create user)
- Creer des volumes nommer de type bind mount (voir discord ismael)